# ====================================
# LOCAL DEVELOPMENT ENVIRONMENT
# Using LocalStack for AWS services
# ====================================

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=local-dev-secret-change-in-production

# Google OAuth (Optional for local dev - you can use email/password)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_ID=your-google-client-id
GOOGLE_SECRET=your-google-client-secret

# AWS DynamoDB Tables (LocalStack)
DOCUMENT_TABLE=curiocity-documents
RESOURCE_TABLE=curiocity-resources
RESOURCEMETA_TABLE=curiocity-resourcemeta

# AWS Configuration (LocalStack - DO NOT CHANGE for local dev)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-west-1
AWS_ENDPOINT_URL=http://localhost:4566

# S3 Configuration (LocalStack)
S3_UPLOAD_REGION=us-west-1
S3_UPLOAD_KEY=test
S3_UPLOAD_SECRET=test
S3_UPLOAD_BUCKET=curiocity-local-bucket

# LlamaCloud API (for file parsing)
# Get API key from: https://cloud.llamaindex.ai/
# Set to 'true' to disable parsing during local development
LLAMA_CLOUD_API_KEY=your-llama-api-key
DISABLE_PARSING=true

# PostHog Analytics (Optional for local dev)
# Get these from: https://posthog.com/
NEXT_PUBLIC_POSTHOG_KEY=your-posthog-key
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ====================================
# NOTES:
# ====================================
# 1. Copy this file to .env.local: cp .env.local.example .env.local
# 2. For local development with LocalStack, the AWS credentials can remain as "test"
# 3. Google OAuth is optional - you can sign up with email/password
# 4. LlamaCloud API is optional - set DISABLE_PARSING=true to skip file parsing
# 5. PostHog is optional for local development
