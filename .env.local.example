# ====================================
# APEX - LOCAL DEVELOPMENT ENVIRONMENT
# PostgreSQL + NextAuth + File Storage
# ====================================

# Database
# For local development, use Docker PostgreSQL via docker-compose
# Apex uses port 54320 to avoid conflicts with other PostgreSQL instances
DATABASE_URL="postgresql://postgres:devpassword@localhost:54320/apex_dev"

# NextAuth Configuration
# Apex uses port 3100 to avoid conflicts with other Next.js apps
NEXTAUTH_URL="http://localhost:3100"
NEXTAUTH_SECRET="generate-this-with-openssl-rand-base64-32"

# Google OAuth (Optional)
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-your-client-secret"

# LinkedIn OAuth (Optional)
# Get credentials from: https://www.linkedin.com/developers/apps
LINKEDIN_CLIENT_ID="your-linkedin-client-id"
LINKEDIN_CLIENT_SECRET="your-linkedin-client-secret"

# Resend (for magic link emails)
# Get API key from: https://resend.com/
RESEND_API_KEY="re_your_api_key"
# Email address to send magic links from
RESEND_FROM_EMAIL="noreply@yourdomain.com"

# LlamaCloud API (for file parsing)
# Get API key from: https://cloud.llamaindex.ai/
LLAMA_CLOUD_API_KEY="llx-your-api-key"

# PostHog Analytics (Optional for local dev)
# Get these from: https://posthog.com/
NEXT_PUBLIC_POSTHOG_KEY="phc_your_key"
NEXT_PUBLIC_POSTHOG_HOST="https://app.posthog.com"

# File Storage
# Local filesystem path for document storage
STORAGE_PATH="./storage"

# ====================================
# QUICK START:
# ====================================
# 1. Copy this file: cp .env.local.example .env.local
# 2. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 3. Run setup script: npm run local:setup
# 4. Start dev server: npm run dev
#
# ====================================
# NOTES:
# ====================================
# - Google OAuth and LinkedIn OAuth are optional (for social login)
# - Resend API is required only for magic link authentication
# - LlamaCloud API is optional (for AI document parsing)
# - PostHog is optional (for analytics)
# - All optional services can be left as placeholder values for basic development
