# ====================================
# APEX - LOCAL DEVELOPMENT ENVIRONMENT
# PostgreSQL + NextAuth + File Storage
# ====================================

# Database
# For local development, use Docker PostgreSQL:
# docker run -d --name apex-db -p 5432:5432 \
#   -e POSTGRES_PASSWORD=devpassword -e POSTGRES_DB=apex_dev \
#   -v apex-pgdata:/var/lib/postgresql/data postgres:16-alpine
DATABASE_URL="postgresql://postgres:devpassword@localhost:5432/apex_dev"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-this-with-openssl-rand-base64-32"

# Resend (for magic link emails)
# Get API key from: https://resend.com/
RESEND_API_KEY="re_your_api_key"
# Email address to send magic links from
RESEND_FROM_EMAIL="noreply@yourdomain.com"

# LlamaCloud API (for file parsing)
# Get API key from: https://cloud.llamaindex.ai/
LLAMA_CLOUD_API_KEY="llx-your-api-key"

# PostHog Analytics (Optional for local dev)
# Get these from: https://posthog.com/
NEXT_PUBLIC_POSTHOG_KEY="phc_your_key"
NEXT_PUBLIC_POSTHOG_HOST="https://app.posthog.com"

# File Storage
# Local filesystem path for document storage
STORAGE_PATH="./storage"

# ====================================
# NOTES:
# ====================================
# 1. Copy this file to .env.local: cp .env.local.example .env.local
# 2. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 3. Start PostgreSQL: docker run -d --name apex-db -p 5432:5432 \
#      -e POSTGRES_PASSWORD=devpassword -e POSTGRES_DB=apex_dev \
#      -v apex-pgdata:/var/lib/postgresql/data postgres:16-alpine
# 4. Run migrations: npx prisma migrate dev
# 5. Resend API is required for magic link authentication
# 6. LlamaCloud API is required for document parsing
# 7. PostHog is optional for local development
